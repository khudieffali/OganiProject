 @model IEnumerable<Category>

<!-- [ basic-table ] start -->
<div class="col-md-12">
    <div class="card">
        <div class="card-header">
            <h5>Kateqoriyalar</h5>
            <div class="btnCreate">
                <a asp-controller="Categories" asp-action="Create">
                    <button class="btn btn-primary">Əlavə et</button>
                </a>
            </div>
        </div>
        <div class="card-body table-border-style">
            <div class="table-responsive">
                <table class="tree table table-bordered table-dark" id="myTableOne">
                    <thead class="table-primary">
                        <tr>
                            <th>Şəkil</th>
                            <th>Adı</th>
                            <th>Parametrlər</th>
                        </tr>
                    </thead>
                    <tfoot id="CategoryList">
                        @await Html.PartialAsync("_PartialCategory")
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
<!-- [ basic-table ] end -->
<!-- [ inverse-table ] start -->
@section scripts {
    <script src="/lib/jquery-treegrid/js/jquery.treegrid.js"></script>
    <script>
        $('.tree').treegrid().treegrid('collapseAll');
    </script>
    <script>
        function removeEntity(id) {
            Swal.fire({
                title: "Silmək istədiyinizə əminsiniz?",
                icon: "warning",
                showCancelButton: true,
                confirmButtonColor: "#3085d6",
                cancelButtonColor: "#d33",
                cancelButtonText: "Ləğv et!",
                confirmButtonText: "Bəli, əminəm!"
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        method: "POST",
                        url: "@Url.Action("delete", "categories")",
                        data: { id },
                        success: (result) => {
                            if (!result.error) {
                                Swal.fire({
                                    title: "Uğurla Silindi!",
                                    icon: "success"
                                });
                                $("#CategoryList").html(result)

                                return;
                            }
                            Swal.fire({
                                title: "Silinmə Uğursuz oldu!",
                                icon: "error"
                            });
                        },
                        error: (error) => {
                            Swal.fire({
                                title: "Silinmə Uğursuz oldu!",
                                icon: "error"
                            });
                        }
                    })
                }
            });
        }
    </script>
 
}